#include <bits/stdc++.h>
using namespace std;

using ll = long long;
using ld = long double;

int main()
{
    string a,b,c;
    cin >> a >> b >> c;

    cout << a.at(0) << b.at(1) << c.at(2) << endl;

    return 0;
}